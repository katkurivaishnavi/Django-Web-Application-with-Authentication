# Django Authentication Web Application

## Overview
A Django-based web application that implements user authentication including registration, login, logout, password reset, and role-based access using Django’s built-in authentication system.

## Features
- User Registration
- Login & Logout
- Password Reset
- Role-based access (Admin / User)
- Protected Dashboard
- Secure Authentication

## Tech Stack
- Python
- Django
- HTML
- SQLite

## How to Run
```bash
venv\Scripts\activate
pip install django
python manage.py makemigrations
python manage.py migrate
python manage.py runserver

  
